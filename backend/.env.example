# MongoDB connection URI. Replace <user>, <password>, and <cluster> with your values.
# Example uses the `atlaswatch` database explicitly and enables retryWrites.
MONGODB_URI="mongodb+srv://<user>:<password>@<cluster>.mongodb.net/atlaswatch?retryWrites=true&w=majority"

# Optional: port your host provides. Defaults to 3000 when not set.
PORT=3000
